# API
fastapi==0.115.5
uvicorn[standard]==0.32.1
# Required by FastAPI for multipart/form-data parsing — bundle uploads use
# Form() + UploadFile, which import this lazily and crash on first request
# if it's missing.
python-multipart==0.0.18
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Neo4j
neo4j==5.27.0

# LangChain / LangGraph
langchain==0.3.13
langchain-openai==0.3.0
langchain-community==0.3.13
langgraph==0.2.60

# OpenAI
openai==1.59.6

# Ontology
rdflib==7.1.1
pyshacl==0.27.0
pyyaml==6.0.2

# Utilities
httpx==0.28.1
tenacity==9.0.0

# Datasource connectors
# psycopg v3 is the modern Postgres driver. Bundle the C-compiled binary
# wheel so installation doesn't require libpq dev headers. Lazy-imported in
# pipeline/datasources/postgres.py — bundles that don't reference postgres
# datasources still work without it.
psycopg[binary]==3.2.3

# Testing
pytest==8.3.4
